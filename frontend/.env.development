# Demo mode: il frontend gira con dati simulati (MSW) e login finta, senza
# backend né Entra ID. `npm run dev` è autosufficiente per la presentazione.
# Per usare il backend reale, crea un .env.development.local con VITE_USE_MSW=false.
VITE_USE_MSW=true
